# Frontend

## Environment variables
Create a `.env` file in this directory with:

```
VITE_MAPBOX_TOKEN=your_mapbox_access_token
```

This token is used by the Crime News map to render the Mapbox map.
